{% extends "layout.html.twig" %}

{% block content %}
    <h1>Index</h1>
    <div class="content" contenteditable="true">
        {{ note.html|raw }}
        <br>
        {# <textarea id="note" rows="10" cols="50">{{ note.markdown }}</textarea> #}
    </div>
{% endblock %}